$placeholder-color = #b9b9b9

// Field + label
.form-item
	margin-bottom 15px

label
	display block
	font-size 0.8rem
	font-weight 700

select
.select
textarea
input[type="text"]
input[type="password"]
input[type="datetime"]
input[type="datetime-local"]
input[type="date"]
input[type="month"]
input[type="time"]
input[type="week"]
input[type="number"]
input[type="email"]
input[type="url"]
input[type="search"]
input[type="tel"]
input[type="color"]
.uneditable-input
	display inline-block
	padding 5px 10px
	font-size $font-size-base
